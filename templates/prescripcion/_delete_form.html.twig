<form method="post" action="{{ path('prescripcion_delete', {'id': prescripcion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prescripcion.id) }}">
    <button class="btn">Delete</button>
</form>
