{% extends 'base.html.twig' %}

{% block title %}{{ ubicacion is defined ? 'Editar Ubicación' : 'Agregar Nueva Ubicación' }}{% endblock %}
{% block panelTitle %}{{ ubicacion is defined ? 'Editar Ubicación' : 'Agregar Nueva Ubicación' }}{% endblock %}

{% block body %}

{{ form_start(form) }}
    {{ form_row(form.nombre, { 'attr': {} }) }}
    
    <div id="ubicacion-warning" class="text-danger mt-2" style="display: none;">
        Esta ubicación ya existe. Por favor, elige un nombre diferente.
    </div>

    {{ form_row(form.descripcion, { 'attr': {} }) }}
    <button type="submit" id="submit-btn" class="btn btn-success">{{ ubicacion is defined ? 'Actualizar' : 'Guardar' }}</button>
{{ form_end(form) }}

<a href="{{ path('app_ubicacion_index') }}" class="btn btn-secondary mt-3">Volver al Listado</a>

<script>
  {{ parent() }}
    const ubicacionInput = document.getElementById('ubicacion_nombre');
    const warning = document.getElementById('ubicacion-warning');
    const submitBtn = document.getElementById('submit-btn');

    ubicacionInput.addEventListener('input', function () {
        const nombre = this.value;
        if (nombre.length < 2) {
            warning.style.display = 'none';
            submitBtn.disabled = true;
            return;
        }

        fetch(`{{ path('app_ubicacion_validate') }}?nombre=${nombre}`)
            .then(response => response.json())
            .then(data => {
                if (data.exists) {
                    warning.style.display = 'block';
                    submitBtn.disabled = true;
                } else {
                    warning.style.display = 'none';
                    submitBtn.disabled = false;
                }
            });
    });
</script>
{% endblock %}