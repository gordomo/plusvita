<form method="post" action="{{ path('evolucion_delete', {'id': evolucion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evolucion.id) }}">
    <button class="btn">Delete</button>
</form>
