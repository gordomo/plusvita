{% extends 'base.html.twig' %}

{% block title %}Evoluciones{% endblock %}

{% block body %}
<div class="container">
    <div class="">
        <h1>Agregar Evolucion para {{ nombreCliente }}</h1>

        {{ include('evolucion/_form.html.twig') }}

    </div>

</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        {% if errors is not empty %}
        alert('Asegúrese que ha seleccionado el tipo correcto');
        {% endif %}
    </script>
{% endblock %}
