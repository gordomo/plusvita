{% extends 'base.html.twig' %}

{% block title %}Habitacion{% endblock %}

{% block body %}
    <h1>Habitacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ habitacion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ habitacion.nombre }}</td>
            </tr>
            <tr>
                <th>CamasDisponibles</th>
                <td>{{ habitacion.camasDisponibles }}</td>
            </tr>
            <tr>
                <th>CamasOcupadas</th>
                <td>{{ habitacion.camasOcupadas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('habitacion_index') }}">back to list</a>

    <a href="{{ path('habitacion_edit', {'id': habitacion.id}) }}">edit</a>

    {{ include('habitacion/_delete_form.html.twig') }}
{% endblock %}
