<form method="post" action="{{ path('adjuntos_pacientes_delete', {'id': adjuntos_paciente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adjuntos_paciente.id) }}">
    <button class="btn">Delete</button>
</form>
