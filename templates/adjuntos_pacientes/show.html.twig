{% extends 'base.html.twig' %}

{% block title %}AdjuntosPacientes{% endblock %}

{% block body %}
    <h1>AdjuntosPacientes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adjuntos_paciente.id }}</td>
            </tr>
            <tr>
                <th>Id_paciente</th>
                <td>{{ adjuntos_paciente.idPaciente }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ adjuntos_paciente.url }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adjuntos_paciente.nombre }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ adjuntos_paciente.tipo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adjuntos_pacientes_index') }}">back to list</a>

    <a href="{{ path('adjuntos_pacientes_edit', {'id': adjuntos_paciente.id}) }}">edit</a>

    {{ include('adjuntos_pacientes/_delete_form.html.twig') }}
{% endblock %}
