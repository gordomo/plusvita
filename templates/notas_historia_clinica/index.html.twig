{% extends 'base.html.twig' %}

{% block title %}NotasHistoriaClinica index{% endblock %}

{% block body %}
    <h1>NotasHistoriaClinica index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Text</th>
                <th>Fecha</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for notas_historia_clinica in notas_historia_clinicas %}
            <tr>
                <td>{{ notas_historia_clinica.id }}</td>
                <td>{{ notas_historia_clinica.text }}</td>
                <td>{{ notas_historia_clinica.fecha ? notas_historia_clinica.fecha|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('notas_historia_clinica_show', {'id': notas_historia_clinica.id}) }}">show</a>
                    <a href="{{ path('notas_historia_clinica_edit', {'id': notas_historia_clinica.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('notas_historia_clinica_new') }}">Create new</a>
{% endblock %}
