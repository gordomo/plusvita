{% extends 'base.html.twig' %}

{% block title %}TipoItem{% endblock %}

{% block body %}
    <h1>TipoItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_item.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_item.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipo_item.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipo_item_index') }}">back to list</a>

    <a href="{{ path('app_tipo_item_edit', {'id': tipo_item.id}) }}">edit</a>

    {{ include('tipo_item/_delete_form.html.twig') }}
{% endblock %}
